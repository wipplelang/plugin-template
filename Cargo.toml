[package]
name = "plugin"
version = "0.0.0"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
wipple = { git = "https://github.com/wipplelang/wipple" }
wipple_plugins = { git = "https://github.com/wipplelang/wipple" }
